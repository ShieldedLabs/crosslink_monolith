- On `s1_dev`: Document the requirement. Wherever snapshots, backups, or resyncs are described: state and pos.chain must be backed up and restored together, from the same stopped node. Never pair a non-genesis state with a fresh pos.chain. Community snapshot archives should ship both.
- maybe the WAL/db for tenderlink/pos should be a different file from pow chain, not the same rocksdb db: featurenet participants sometimes delete their pow chain, but the pos chain is uniquely consensus-relevant in that a finalizer must WAL their votes to prevent themselves from ever equivocating, and to satisfy tendermint paper safety invariants
- Nightly: node crash-loops on every restart past h400 (inherited Zcash Testnet checkpoint list) https://github.com/ShieldedLabs/crosslink_monolith/issues/70
- Detect this case instead of deferring forever: when the referenced BFT block is a bootstrap genesis pointer (sigs:0) at the activation height and it does not equal the genesis this node derived, that is permanent, not "not resolvable yet". Logging both hashes once, with the likely cause, would turn a silent hang into a one-line diagnosis. https://github.com/ShieldedLabs/crosslink_monolith/issues/71
- Consider making the bootstrap parameters, including sigma, part of the per-network parameters that a joining node validates and reports on mismatch, so an incompatible build fails loudly at the activation height. https://github.com/ShieldedLabs/crosslink_monolith/issues/71
- fix the difficulty sawtooth (testnet min-difficulty "escape hatch" for mining)
- As a new user, how can we tell we are setup correctly? This needs to improve. If we stop being setup correctly, for whatever reason, how would an operator know? This is the type of weather I like to monitor. USCMig's get_tfl_quorum_status goes finalizer by finalizer to determine each person's status - If we can add a status button in the main GUI in the finalizer section with that, perfect. Simple "synced and ready" or "not ready" so its dead easy to see. You can simplify the right hand pane; if I dont see my validator in the main list, somethings wrong. Not sure if that is possible. Then we can go into advance mode, to figure out why.
